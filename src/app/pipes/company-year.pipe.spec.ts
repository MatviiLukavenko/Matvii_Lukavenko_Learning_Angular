import { CompanyYearPipe } from './company-year.pipe';

describe('CompanyYearPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanyYearPipe();
    expect(pipe).toBeTruthy();
  });
});
